---
paths:
  - "src/controllers/**/*.ts"
---
# コントローラ開発ルール

- すべてのコントローラは `Controller` 基底クラスを継承する
- コントローラは外部依存を持たない（ゼロ依存ライブラリ）
- DOM 操作は直接行い、フレームワーク固有の API は使わない
- 新しいコントローラ追加時は `src/index.ts` の export も更新する
