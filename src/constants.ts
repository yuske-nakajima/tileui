// SVG ノブ関連の定数

/** ノブの可動範囲（度） */
export const KNOB_RANGE_DEG = 270;

/** ノブの開始角度（下部の隙間が均等になるように） */
export const KNOB_START_DEG = 135;

/** ノブの半径 */
export const KNOB_RADIUS = 40;

/** SVG viewBox 内の中心座標 */
export const KNOB_CENTER = 50;

/** SVG viewBox のサイズ */
export const KNOB_SVG_SIZE = 100;

/** タイルのデフォルトサイズ（px） */
export const TILE_SIZE = 100;

/** ノブの軌跡（トラック）の線幅 */
export const KNOB_TRACK_WIDTH = 4;

/** ノブの値インジケーターの線幅 */
export const KNOB_VALUE_WIDTH = 4;

/** ノブのつまみ（サム）の半径 */
export const KNOB_THUMB_RADIUS = 6;

/** スタイル注入用の ID */
export const STYLE_ELEMENT_ID = 'tileui-styles';

/** CSS クラス名のプレフィックス */
export const CSS_PREFIX = 'tileui';
